/**
 * Study Capture API (Next.js backend). Change this if your dev server uses another host/port.
 * Align with `.env` `NEXT_PUBLIC_STUDY_CAPTURE_API_URL` in the SmartAI repo when applicable.
 */
export const STUDY_CAPTURE_API_URL =
  "http://localhost:3000/api/study-capture/analyze";
